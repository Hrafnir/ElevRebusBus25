SUPABASE_URL=https://mbdfibkufvpssbemkylq.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional for local fallback until Supabase auth is fully wired into the UI.
ALLOW_DEV_AUTH=true

# Optional shared app-level Google Maps key.
# Later, organizations can override this with their own restricted key.
GOOGLE_MAPS_API_KEY=
